---
_meta:
  type: "roles"
  config_version: 2

external_user:
  index_permissions:
    - index_patterns:
        - "documents"
      allowed_actions:
        - "indices:data/read"
        - "indices:data/read/search"

internal_worker:
  cluster_permissions:
    - "indices:data/write/bulk"
  index_permissions:
    - index_patterns:
        - "documents"
      allowed_actions:
        - "write"
        - "indices:admin/create"

